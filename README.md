# JKS Certificate Url Generator

Generates a JKS Certificate that imports all certificates gathered from the url list.

Prerequisite:

- pip install -r requirements.txt
- sudo apt install openjdk-14-jre-headless